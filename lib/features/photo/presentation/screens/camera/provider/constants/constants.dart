const int countDownSeconds = 3;
const int countDownPeriod = 1;
const Duration controllerDisposeDelay = Duration(milliseconds: 100);
const Duration permissionRequestDelay = Duration(milliseconds: 100);
